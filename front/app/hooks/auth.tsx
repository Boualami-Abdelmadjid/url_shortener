import React from "react";
import { LoginBody,RegisterBody } from "../utils/types";


export const useLogin = (body:LoginBody) => {
    console.log(body)
}

export const useRegister = (body:RegisterBody) => {
    console.log(body)
}